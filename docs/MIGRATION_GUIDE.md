# 📦 Руководство по миграции

## Обзор изменений

Игра полностью переписана с монолитного HTML на современную архитектуру:
- **Canvas** для рендеринга игры
- **React** для UI компонентов
- **TypeScript** для типизации

## Сравнение архитектур

### Старая версия (index_old.html)
```
index.html (5100+ строк)
├── HTML разметка
├── CSS стили
└── JavaScript логика
    ├── Игровой движок
    ├── Рендеринг
    ├── UI
    ├── Боты
    └── Мультиплеер
```

### Новая версия
```
src/
├── components/          # React UI
│   ├── MainMenu.tsx     # Главное меню
│   ├── GameSetup.tsx    # Настройки
│   ├── GameCanvas.tsx   # Canvas обертка
│   ├── ScoreBoard.tsx   # Счет
│   ├── PauseMenu.tsx    # Пауза
│   └── GameOver.tsx     # Конец игры
├── core/                # Игровая логика
│   ├── GameEngine.ts    # Движок (боты, физика)
│   ├── Renderer.ts      # Отрисовка Canvas
│   └── InputHandler.ts  # Ввод (клавиатура, тач)
├── types/               # TypeScript типы
├── constants/           # Константы
└── styles/              # CSS модули
```

## Преимущества новой архитектуры

### 1. Разделение ответственности
- **GameEngine** - только логика игры
- **Renderer** - только отрисовка
- **React** - только UI

### 2. Типизация TypeScript
```typescript
interface Player {
  snake: Snake;
  score: number;
  type: PlayerType;
  control?: ControlType;
}
```

### 3. Переиспользование
```tsx
<ScoreBoard players={players} />
<PauseMenu onResume={resume} onMainMenu={menu} />
```

### 4. Тестируемость
Каждый модуль можно тестировать отдельно

### 5. Производительность
Canvas рендеринг быстрее DOM манипуляций

## Что сохранено?

✅ Все игровые режимы:
- Классика
- Полупризрак
- Семейка недопризраков
- Полноценный призрак
- Компашка призраков
- Магический шутер

✅ Все функции:
- До 8 игроков (4 человека + 4 бота)
- Умные боты с 5 стратегиями
- Мультиплеер через WebSocket
- Все визуальные эффекты
- Частицы, пули, призраки

✅ Управление:
- 4 схемы клавиатуры
- Сенсорное управление
- Пауза, полный экран

## Как работает новая версия?

### 1. Запуск игры
```typescript
// App.tsx
const handleStartGame = (humanCount, botCount, gameMode, controls) => {
  engineRef.current = new GameEngine();
  engineRef.current.initGame(humanCount, botCount, gameMode, controls);
  setCurrentScreen('game');
};
```

### 2. Игровой цикл
```typescript
// GameCanvas.tsx
const gameLoop = (timestamp: number) => {
  if (timestamp - lastUpdateTime >= GAME_SPEED) {
    engine.update();  // Обновление логики
    lastUpdateTime = timestamp;
  }
  renderer.render(engine.getState());  // Отрисовка
  requestAnimationFrame(gameLoop);
};
```

### 3. Обработка ввода
```typescript
// InputHandler.ts
document.addEventListener('keydown', (e) => {
  if (e.key === 'w') {
    onDirectionChange(0, DIRECTIONS.UP);
  }
});
```

## Добавление новых функций

### Пример: Новый режим игры

1. Добавьте тип в `types/game.ts`:
```typescript
export type GameMode = 'classic' | 'new-mode';
```

2. Добавьте логику в `GameEngine.ts`:
```typescript
if (this.state.gameMode === 'new-mode') {
  // Ваша логика
}
```

3. Добавьте кнопку в `GameSetup.tsx`:
```tsx
<button onClick={() => setGameMode('new-mode')}>
  Новый режим
</button>
```

## Мультиплеер

Сервер на Python остался без изменений:
```bash
python server.py
```

Интеграция WebSocket будет добавлена в следующей версии.

## Миграция данных

Старая версия не использовала localStorage, поэтому миграция не требуется.

## Обратная совместимость

Старый файл `index_old.html` полностью функционален и может использоваться параллельно.

## Следующие шаги

1. ✅ Базовая архитектура
2. ✅ Все игровые режимы
3. ✅ UI компоненты
4. 🔄 Интеграция мультиплеера
5. 🔄 Звуковые эффекты
6. 🔄 Сохранение настроек
7. 🔄 Достижения

## Вопросы?

Сравните файлы:
- `index_old.html` - старая версия (5100 строк)
- `src/` - новая версия (модульная)

Оба варианта работают и доступны!
