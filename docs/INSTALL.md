# 📦 Установка и запуск

## Требования

### Обязательно
- **Node.js** версии 16 или выше
- **npm** (устанавливается вместе с Node.js)

### Опционально (для мультиплеера)
- **Python** 3.7+
- **pip** (для установки зависимостей Python)

## Проверка установленных версий

```bash
node -v    # Должно быть v16.0.0 или выше
npm -v     # Любая версия
python -V  # Опционально, для мультиплеера
```

## Установка Node.js

### macOS
```bash
# Через Homebrew
brew install node

# Или скачайте с официального сайта
# https://nodejs.org/
```

### Windows
Скачайте установщик с https://nodejs.org/ и запустите его

### Linux
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nodejs npm

# Fedora
sudo dnf install nodejs npm
```

## Установка зависимостей проекта

```bash
# В корневой директории проекта
npm install
```

Это установит:
- React 18.2
- React DOM 18.2
- TypeScript 5.3
- Vite 5.0
- И другие зависимости

## Запуск игры

### Вариант 1: Автоматический (macOS/Linux)
```bash
./START.sh
```

### Вариант 2: Ручной
```bash
npm run dev
```

### Вариант 3: Продакшн
```bash
npm run build
npm run preview
```

## Открытие в браузере

После запуска откройте:
```
http://localhost:3000
```

## Мультиплеер (опционально)

### 1. Установите Python зависимости
```bash
pip install -r requirements.txt
```

### 2. Запустите сервер
```bash
python server.py
```

### 3. Подключитесь к серверу
В игре выберите "Мультиплеер" и используйте:
```
ws://localhost:8765
```

## Возможные проблемы

### Ошибка: "node: command not found"
**Решение:** Установите Node.js (см. выше)

### Ошибка: "Cannot find module"
**Решение:** 
```bash
rm -rf node_modules package-lock.json
npm install
```

### Ошибка: "Port 3000 is already in use"
**Решение:** 
```bash
# Остановите процесс на порту 3000
# macOS/Linux
lsof -ti:3000 | xargs kill -9

# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

### Игра не загружается в браузере
**Решение:**
1. Очистите кэш браузера (Ctrl+Shift+Delete)
2. Попробуйте другой браузер
3. Проверьте консоль браузера (F12) на ошибки

### Старая версия работает, новая нет
**Решение:**
Используйте старую версию `index_old.html` пока разбираетесь с проблемой

## Структура после установки

```
node_modules/          # Зависимости (создается после npm install)
src/                   # Исходный код
dist/                  # Сборка (создается после npm run build)
index.html             # Точка входа
package.json           # Конфигурация npm
tsconfig.json          # Конфигурация TypeScript
vite.config.ts         # Конфигурация Vite
```

## Команды npm

```bash
npm run dev      # Запуск в режиме разработки
npm run build    # Сборка для продакшн
npm run preview  # Превью продакшн сборки
```

## Горячие клавиши разработки

При запуске `npm run dev`:
- Изменения в коде применяются автоматически (HMR)
- Не нужно перезагружать страницу
- Ошибки показываются в браузере

## Следующие шаги

1. ✅ Установили Node.js
2. ✅ Установили зависимости (`npm install`)
3. ✅ Запустили игру (`npm run dev`)
4. ✅ Открыли в браузере (http://localhost:3000)
5. 🎮 Играйте!

## Дополнительная помощь

- [QUICKSTART_NEW.md](QUICKSTART_NEW.md) - Быстрый старт
- [README.md](README.md) - Основная документация
- [MIGRATION_GUIDE.md](MIGRATION_GUIDE.md) - Сравнение версий

## Контакты

Если ничего не помогло:
1. Проверьте версию Node.js (должна быть 16+)
2. Попробуйте старую версию `index_old.html`
3. Создайте issue в репозитории проекта
