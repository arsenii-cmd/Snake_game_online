# 🎮 Мультиплеер для игры "Змейка"

## 📝 Описание

Игра теперь поддерживает онлайн-мультиплеер через WebSocket. Несколько игроков могут играть вместе на разных устройствах в реальном времени!

## ✨ Возможности

- ✅ Игра в реальном времени с другими игроками
- ✅ Поддержка всех режимов игры (Классика, Призрак, Магический шутер)
- ✅ Автоматическая синхронизация позиций
- ✅ Поддержка клавиатуры и сенсорного управления
- ✅ Игра через локальную сеть или интернет

## Запуск сервера

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Запустите сервер:
```bash
python server.py
```

Сервер запустится на `ws://localhost:8765`

## Тестирование подключения

Перед запуском игры рекомендуется проверить работу сервера:

### Вариант 1: Python скрипт
```bash
python test_server.py
```

### Вариант 2: HTML страница
Откройте `test_connection.html` в браузере и нажмите "Подключиться"

## Подключение к игре

1. Откройте `index.html` в браузере
2. Выберите режим "Мультиплеер"
3. Введите адрес сервера (по умолчанию `ws://localhost:8765`)
4. Нажмите "Настроить управление" и затем "Начать игру"

## Особенности мультиплеера

- Каждый игрок управляет своей змейкой
- Позиции всех игроков синхронизируются в реальном времени
- Поддерживаются все режимы игры (Классика, Призрак, Магический шутер)
- При отключении игрока его змейка исчезает с поля

## Настройка сервера

В файле `server.py` можно изменить:
- `host` - адрес сервера (по умолчанию "localhost")
- `port` - порт сервера (по умолчанию 8765)

Для игры через интернет:
1. Измените `host` на `"0.0.0.0"` в `server.py`
2. Откройте порт 8765 в файерволе
3. Используйте внешний IP-адрес сервера для подключения

## Протокол обмена сообщениями

### Клиент → Сервер

**Присоединение:**
```json
{
  "type": "join",
  "player_id": "unique_id",
  "name": "Player Name"
}
```

**Обновление позиции:**
```json
{
  "type": "update",
  "data": {
    "x": 10,
    "y": 15,
    "score": 50,
    "direction": {"x": 1, "y": 0},
    "length": 6
  }
}
```

**Действие игрока:**
```json
{
  "type": "action",
  "action": "shoot",
  "data": {
    "x": 10,
    "y": 15,
    "direction": {"x": 1, "y": 0}
  }
}
```

### Сервер → Клиент

**Подтверждение подключения:**
```json
{
  "type": "joined",
  "player_id": "unique_id",
  "game_state": {
    "players": {},
    "started": false
  }
}
```

**Новый игрок:**
```json
{
  "type": "player_joined",
  "player_id": "unique_id",
  "player": {
    "name": "Player Name",
    "x": 0,
    "y": 0,
    "score": 0
  }
}
```

**Обновление игрока:**
```json
{
  "type": "player_update",
  "player_id": "unique_id",
  "data": {
    "x": 10,
    "y": 15,
    "score": 50
  }
}
```

**Игрок покинул игру:**
```json
{
  "type": "player_left",
  "player_id": "unique_id"
}
```

## Известные ограничения

- Синхронизация еды пока не реализована (каждый клиент генерирует свою еду)
- Столкновения между игроками обрабатываются локально
- Нет авторитетного сервера для проверки действий

## Планы развития

- [ ] Синхронизация еды между клиентами
- [ ] Авторитетный сервер для проверки столкновений
- [ ] Комнаты для игр
- [ ] Чат между игроками
- [ ] Рейтинговая система
